version: 3

project:
  locator: SolaceLabs_solace-mc-intellij-plugin
  id: SolaceLabs_solace-mc-intellij-plugin
  name: solace-mc-intellij-plugin
  teams: []
  labels:
    - java

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
